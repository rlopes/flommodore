//! Flommodore — `encode.zig` (Block 2).
//! Gab-16 instruction encoder — one source of encoding truth, shared by test-ROM generation (Block 2) and the flas assembler backend (Block 10).
//! Stub created in Block 1 (task 1.3); implementation lands in Block 2.

pub fn init() void {}
