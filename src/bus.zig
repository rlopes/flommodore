//! Flommodore — `bus.zig` (Block 2).
//! Memory bus: 20-bit address routing, open bus, ROM shadow window.
//! Stub created in Block 1 (task 1.3); implementation lands in Block 2.

pub fn init() void {}
