//! Flommodore — `tests/harness.zig` (Block 2).
//! Headless test runner: loads a generated test ROM, runs the machine with
//! no SDL and fixed seeds, and checks completion state (plan task 2.8).
//! Stub created in Block 1 (task 1.3).

pub fn init() void {}
